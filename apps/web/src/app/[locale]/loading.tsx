import { RouteSegmentLoadingFallback } from "@/components/RouteSegmentLoadingFallback";
import type { FC } from "react";

const Loading: FC = () => <RouteSegmentLoadingFallback />;
export default Loading;
